# Prompt History

## 2026-08-31 — xnat_monai_bundle_runner
- Initial scaffold from AI foundation-models planning session: generic Container
  Service command wrapping `monai.bundle download` + `run` so any Model Zoo bundle
  is launchable from XNAT. Chosen as highest leverage-per-hour item in
  `business/ai-foundation-models/MODEL-CATALOG.md`.
